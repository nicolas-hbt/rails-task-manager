<h1> Task Manager </h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> - <%= task.details %> - <%= link_to "Supprimer", task_path(task), method: :delete, data: { confirm: "Êtes-vous sûr.e ?" } %></li>
  <% end %>
</ul>
